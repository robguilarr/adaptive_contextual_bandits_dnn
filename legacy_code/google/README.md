
[Google CLI installation](https://cloud.google.com/sdk/docs/install)

If gcloud doesn't work, add the following inside `nano ~/.zshrc`.

```text
export PATH=$PATH:/path/to/google-cloud-sdk/bin
```

**OR** 

Credentials Setup:

```text
export GOOGLE_APPLICATION_CREDENTIALS="/Users/Roberto_Aguilar/PycharmProjects/firebase-iap-optimization/google/third-echelon-117-8807429a8a8c.json"
```
